- name: install nginx
  become: yes
  yum: name={{item}} state=present update_cache=yes
  with_items:
  - nginx

- name: change port
  become: yes
  copy:
    src: ./default.conf
    dest: /etc/nginx/conf.d/default.conf

- name: Run nginx
  become: yes
  service:
   name: nginx
   state: restarted
